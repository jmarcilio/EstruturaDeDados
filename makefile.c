/* int main(){

Lista *li;
li = criaLista();
liberaLD(li);
int x = tamLista(li);
int x = estaCheia(li);
if(estaCheia(li))
int x = estaVazia(Lista* li);
if(estaVazia(li))
int x = insereInicio(li, dadosAluno);
int x = insereFim(li, dadosAluno);
int x = insereOrdenado(li, dadosAluno);
int x = removeInicio(li);
int x = removeFim(li);
int x = removeLista(li, matricula_aluno);
int x = consultaPos(li, posicao, &dados_aluno);
int x = consultaMat(li, posicao, &dados_aluno);
} */